# Current task checkpoint

User goal: faithful Total Annihilation recreation first; graphics improvements later. User accepted our engine recommendation: Godot for presentation, independently reconstructed game logic. Keep the original GOG installation intact.

Use Light/low by default, including reverse engineering when it can be done reliably with more time. The user permits Medium only for a concrete problem that truly requires it, followed immediately by Light. No other modes or model substitutions are authorized. Do not escalate by broad task category.

## Workflow

Codex investigates, plans, and reviews; Claude implements assigned changes. A CLI session can be continued by Codex without losing context. Do not commit credentials or private session identifiers to tracked files.

## Completed

User reiterated (September 13): the ultimate goal is a playable full port of Total Annihilation, and every verified checkpoint must be committed and pushed to GitHub.

Squads and command research: viewer control groups per recovered 0x48d920/0x48d9a0 (Ctrl+1..9 assign, Alt+1..9 select, Shift add, Ctrl+A, Ctrl+Z, Esc) with --verify-squads both factions; sidebar minimap (minimap.gd). Command-layer research (order record 0x56 bytes, 67-entry sorted order table, 0x43adc0 insert/marker rules, shift toggle 0x43afc0, dispatcher 0x43b7c0 return codes, background list 0x43bad0, stances f110 bits 18-21, keyboard map) saved as the unified order-queue plan; implement after the COB VM parity checkpoint. See PLAYER_CONTROLS.md.

LOS foundation: prepare_visibility.py extracts LOS.TDF and VISMASKS.GAF (10 vismask frames, header version 0); los_tables.gd ports the 0x433130 ray loader incl. its TDF parser quirks (rays grouped by rotation, not interleaved) - native 33,899/33,899 (audited, 5 parser bugs fixed); los_height_grid.gd ports 0x482c20 - native 244/244 grids (568,416 bytes, full branch coverage). Both in -Native. See LOS_FOUNDATION.md. Next: stamping/lifecycle (C4) and visibility queries (C6) are in progress as workflows, then world hook, radar/jam/cloak, targeting gates, fog rendering.

Skirmish start: the computer player starts with its own world-scripted Commander (ConstructionWorld.add_unit scripted_commander) at OTA start 2, guarding and building its base (opponent policy counts it as a builder and does not send it to attack); schema HumanMetal/HumanEnergy and ComputerMetal/ComputerEnergy applied. --verify-skirmish-start on Sherwood/The Cold Place in NORMAL; --verify-opponent still passes. Original AI research workflow running (data files + build/military decision loop).

Combat orders: research workflow recovered 0x499cd0/0x489bb0/0x489ce0 damage notifications (HitByWeapon(400cos,400sin of the hit-angle high byte) then TakeDamage(pct) as queued threads for surviving type-1 hits, none on lethal hits; ARMORED damagemodifier), self-destruct order 0x402010 (countdown & 7 default 5, 30-tick counts, rand(15) final wait, reason-3 death with selfdestructas, cancel rules incl. detonation after expiry), and Suppress ground attack 0x4038a0/0x48a1e0 (integer X/Z point, Y = max(terrain, sea), repeats until stopped, command fire completes after one shot, R-shrinking approach). damage_notify.gd + CombatWorld.apply_damage(source_raw, damage_type), toggle/step_self_destructs, attack_ground/command_fire_ground/ground_target/step_ground_approach; viewer Ctrl+D, G+click, D-gun on empty ground. Native oracle native_hit_notify.py 18,601/18,601 over 3,720 cases, audited (-Native); test_damage_notify (19), test_self_destruct (12), test_ground_attack (15), --verify-self-destruct and --verify-ground-attack both factions in NORMAL. Veterancy added: kill word +0xb8 credited at 0x4869ca (fully built victim, attacker of another owner), attacker +6%/level and target -4%/level damage (test_damage_notify 25). Paralyze orders, order queue cycling and 0x49aa80 range check not modelled. See COMBAT_ORDERS.md. Next: line of sight/radar/fog (research plan recovered: per-player LOS byte refcount grid at half cell resolution, mapped bit grid game+0x14273, circular vismask vs true LOS rays from los.tdf, event-driven updates), then level-two factories, aircraft, naval, control groups/command queues, veterancy.

Map feature sprites + schema features: research workflow (analyst + adversarial verifier per topic) recovered 0x436c30/0x423160 OTA schema features (atol XPos/ZPos, case-insensitive lookup, 0x4224b0 append, 2D anchor vs 3D footprint centring, owner 10, after TNT pass 2) and 2D GAF feature loading/animation/draw (seq pointers +0xac..+0xc8, shared per-type state stepped by 0x424050 via 0x4b8b90, 0x46a610 anchor = footprint centre lifted by corner-height sum >> 3, frame hotspot, shadow before main, height<10 pass before units). map_feature_loader schema pass; native oracle runs 0x423160 natively: 1,985/1,985 checks incl. 1,876 schema features over all 96 maps (Seven Islands included via scaled tnt_image). prepare_units extracts 1,212 feature GAF sequences (feature_runtime_version 4); feature_animation.gd + viewer feature_sprite_2d/step_feature_animations. All 96 official maps load (test_maps 480 checks); --verify-map-features on Sherwood (609 sprites) and Acid Pools (animated) in NORMAL. See MAP_FEATURE_SPRITES.md. Next: height-lift projection for units/3D features (with click mapping), burning/reproduction (0x4233a0/0x424050), then radar/fog of war, level-two factories, aircraft, naval, control groups/command queues, HitByWeapon on damage, self-destruct, D-gun ground targeting, skirmish AI.

Skirmish maps: TNT feature loader recovered (0x483a99 pass 1 void 0xfffc, pass 2 row-major 0x423c50 for codes below the feature count; placement removes destructible overlaps, fails on void/indestructible after earlier removals, 0x800 instance pool; 0x47de60 blocking). map_feature_loader.py (+8 unit tests) matches native placement/removal on 1,188/1,188 grids incl. all 95 official multiplayer TNTs (-Native). prepare_maps.py bundles 86 of 96 official Network maps into local/maps/<slug> (index version 2; run_viewer prepares); viewer --map <slug> and sidebar map picker, Commander at OTA start 1, opponent at start 2; ConstructionWorld.load_map_features(placements, voids). test_maps (430 checks, 86 maps) and Sherwood/The Cold Place viewer runs in NORMAL. See SKIRMISH_MAPS.md. Next: draw 2D GAF features (trees/rocks: filename/seqname/seqnameshad in feature TDFs, hotspot placement), OTA schema features (0x423160, 9 maps), then radar/fog of war, level-two factories, aircraft, naval, control groups/command queues, HitByWeapon on damage, self-destruct, D-gun ground targeting.

Feature damage: splash feature pass of 0x49a120 (cell window from areaofeffect>>1, current-cell instance flag selects 0x49a850 instance distance or 0x421eb0 footprint point with 0x485070 bilinear height, 64-entry anchor dedupe) and 0x4244b0 (game option bit 8, indestructible skip, flamable+firestarter 2D ignition, instance +0x26 word or 2D cell word accumulating weapon default damage, 0x423550(x,z,0) featuredead replacement). feature_damage.gd (height/splash/damage + ArrayGrid); FeatureWorld grid interface, terrain-height default instance positions; CombatWorld.blast applies it (GAME_FLAGS 0xc, feature_destructions); navigation rebuild skipped when blocking unchanged; viewer rebuilds replaced sprites. Native oracle 8,668/8,668 (-Native); test_feature_damage (13) and --verify-feature-damage both factions in NORMAL. Burning, animated 2D replacement and feature explosions not implemented. See FEATURE_DAMAGE.md. Next: radar/fog of war, level-two factories, aircraft, naval, maps/skirmish setup, control groups/command queues, HitByWeapon on damage, self-destruct, D-gun ground targeting.

Reclaim: order handler 0x4147b0 recovered (reclaimable flag 0x80 gate; countdown ftol(30 - (metal+energy)*-0.5) from definition values; two-tick sleeps subtracting two; completion 0x4237d0 credits energy/metal to unit income accumulators then 0x423550(x,z,1) swaps in featurereclamate with owner 10). Bundle adds featurereclamate (feature_runtime_version 2). FeatureWorld.replace; ConstructionWorld.reclaim/step_reclaimers/complete_reclaim (credits ledger income, refreshes blocking, records `reclaimed`); viewer click on a reclaimable feature reclaims with the selected builder or Commander. test_reclaim (50) and --verify-reclaim both factions in NORMAL. Approach range (build distance) and state scheduling are provisional; def+0x21c in state 0 is cruisealt (aircraft altitude), not a range. See RECLAIM.md. Next: feature damage/heap replacement (0x423550 param 0), radar/fog of war, level-two factories, aircraft, naval, maps/skirmish setup, control groups/command queues, HitByWeapon on damage, self-destruct, D-gun ground targeting.

Wreckage: feature definitions (1,632, loader layout) and 420 feature models bundled (feature_runtime_version 1); feature_world.gd implements 0x423c50 placement, 0x4246b0 removal and 0x486360 corpse/featuredead selection; ConstructionWorld loads map placements (names now in metal.json) and rebuilds navigation (TerrainNavigation.rebuild) for blocking wrecks; kill_unit places corpses after the death explosion; terrain_impact passes feature codes/heights so projectiles hit wrecks; viewer renders wreck models. test_wreckage (17) and --verify-wreckage both factions in NORMAL; capture inspected. See WRECKAGE.md. Next: reclaim (builders turning features into metal), feature damage/heap replacement, then radar/fog of war, level-two factories, aircraft, naval, more maps.

Unit death: weapon damage goes through CombatWorld.apply_damage (negative signed health, dying flag, damage immunity) and queued kill_unit: severity ((-health*100)/maxdamage + previous 30-tick health percent)/2 clamped 1..100, synchronous Killed(severity) corpse type (0 for unfinished units), removal, then explodeas splash from 0x49b000; deaths chain through the queue and are recorded with corpse feature name/type/debris. cob_vm now implements EXPLODE (vtable+0x34). Killed oracle 352/352 (32 scripts x 11 severities) in -Native; test_unit_death (35) in NORMAL. Corpses are recorded but not yet placed as features. See UNIT_DEATH.md. Next: wreck features (corpse definitions bundle, 0x423c50 placement, blocking, projectile feature collision, rendering) then reclaim.

Group control: viewer Shift+drag box select, A selects army, click terrain = grid formation move, click enemy = group attack, S/right-click = group stop; overlay draws selection rings and the drag box. --verify-group-orders for both factions in NORMAL; capture inspected. See PLAYER_CONTROLS.md. Next candidates: unit death explosions + wreckage features + reclaim, radar/fog of war, level-two (advanced) factories and units, aircraft, naval units, additional maps and a map/skirmish setup screen, control groups and command queues.

D-gun: CombatWorld.command_fire uses a slot-3 (Tertiary) WeaponCycle; WeaponCycle/WeaponQueries take a slot name. Recovered: 0x49e1a0 per-shot energy/metal stock gate and 0x4012a0 immediate payment (stock minus cost, unit requested plus cost); 0x499eb0 removes projectiles only without noexplode 0x400000, AoE<17 unit hits direct else splash 0x49a120, terrain impacts splash. Commander firing oracle now includes tertiary callbacks (327/327 each). test_dgun (21 checks) and --verify-dgun both factions in NORMAL; viewer D key / D-gun button. Ground-point D-gun targeting and stockpile weapons not implemented. See COMMANDER_COMBAT.md.

Commander combat: armcom/corcom primary beam lasers are combat-enabled. Firing scripts match 322/322 each. ConstructionWorld.attach_external registers the viewer-stepped Commander script/mobile (world skips stepping them) and captures its launch offset; CombatWorld.enable_guard(id, pursue=false) is a holding guard (weapon-range acquisition, never stops/moves the unit); attack(..., stop_movement) added; viewer Move/Stop cancel Commander attacks. test_commander_combat (24 checks) and --verify-commander-combat for both factions are in NORMAL. D-gun (command fire, energy per shot, noexplode) not connected. See COMMANDER_COMBAT.md. Next candidates: D-gun, unit death explosions/wreckage/reclaim, multi-unit selection and group orders, radar/fog of war, level-two factories and units, aircraft, naval, additional maps.

Faction-aware opponent: opponent.gd FACTIONS table (arm/core builders, factories, combat products, solar, extractor); Opponent.new(world, combat, team, faction). Viewer Start opponent plays the opposite faction of the player. test_opponent_base --core (16 checks) and --verify-opponent --faction core added to NORMAL; Arm variants still pass. See OPPONENT.md.

Firing spread implemented: combat_world.firing_spread reproduces 0x49d580 for every turret shot — s = (accuracy − (signed16 health << 11)/maxdamage + 0x800) & 0xffff, divided by experience/12 when that exceeds 1 (the instructions are /12, not /6 as first documented), then heading and pitch += bounded_random(s) − (s>>1) on the shared world game RNG (0x4b6c30 = wind Park–Miller). Ballistic launches use the perturbed angles; direct launches only consume the draws. native_cannon_launch.py adds 10 damaged/experienced/inaccurate cases per cannon unit and the host matches launch angles, RNG state, velocity and path; test_firing_spread.gd pins 10 fixtures in NORMAL. Non-turret callbacks 0x49d9c0/0x49db70/0x49dd60 are not inspected for spread yet.

Hammer/Thud short-range overshoot resolved as original behavior: native_cannon_launch.py executes the original 0x49e070 creation offset, the 0x49e1a0 ballistic aim step driving the real AimPrimary script, turret fire callback 0x49d580 (installed at weapon+0x60 by 0x49e010; tolerance gate 0x49d880, muzzle 0x43e240, launcher 0x49cde0) and 0x49b720 flight; compare_native_cannon_launch.gd replays through combat_world's new ballistic_angles/offset_point helpers and matches all 48 engagements (six cannon units × 128/192 × four directions: offset, aim requests, start, velocity, 40-tick path). The original's own Thud/Hammer shells pass 4–9.5 units above the aim point at those ranges. The recovered 0x49d580 firing spread is now implemented (entry above). See CORE_COMBAT.md.

Native projectile terrain collision: the terrain/feature/water branch of 0x49b090 is reconstructed in projectile_collision.gd terrain_contact and matches 1,200 original executions (plus the existing 600 unit cases). Rule: integer y (signed16 of y>>16) below cell byte 6 (lowest corner from 0x483210) impacts, or bounces with vy=-(vy>>2) for groundbounce 0x8000; at or above it, impacts only when under the sea-level byte for a non-waterweapon (0x10000) on a non-lava map; unitsonly 0x4000 skips; feature code at cell+8 (0xfffe continuation via +10/+11 anchor offsets, anchor not count-checked) with definition height +0xfa impacts once per feature cell (cache at projectile +0x5a/+0x5c). All live projectile paths now use WorldCollision.terrain_impact on the floored endpoint cell instead of y < height_at(round(pos/16)). Map features are not supplied to the live world yet (Comet Catcher's are all height 0) and the lava flag is taken as clear. test_projectile_collision now has 18 checks. The laser plateau case changed as predicted: A.K./Jeffy now hit; Weasel/Instigator still end in the plateau's first cell under the native rule. See PROJECTILE_COLLISION.md. Next candidates: supply per-cell feature codes/heights for other maps, the Hammer/Thud short-range overshoot, Core defenses, Core-capable opponent.

Beam lasers: armfav, corfav, corgator and corak are combat-enabled, so every armed Arm and Core level-one ground factory unit can now fight. Recovered: beamweapon = weapon flag 0x8; duration word at +0xf0 (x30, `.02` → 0 ticks; weapon runtime schema 5 adds duration_ticks, launcher and catalog updated); beams use direct launch 0x49c9c0 with tail copied from muzzle and launch tick at +0x42; in 0x49b720 the tail is released once launch+duration < tick and then moves with the head; collision/damage are the ordinary endpoint path. Evidence: loader 1,645/1,645, beam update 600/600 (beam_motion.gd), firing 1,297 snapshots, origins 1,944, targets 648, test_beam_combat 18 checks for each of four units, four real-map factory duels (all in NORMAL; beam oracle and laser firing oracles in -Native). OPEN: host interpolated terrain ends low-muzzle beams at a 24-unit plateau edge where the native cell-byte test probably would not (shared terrain approximation, not beam-specific). Laser rendering is a development line; no capture caught a beam in flight. See BEAM_WEAPONS.md. Next candidates: native terrain/feature projectile collision (would fix that edge case for all weapons), the Hammer/Thud short-range overshoot, Core defenses, Core-capable opponent.

Core T1 combat roster: corthud, corlevlr, corstorm, cormist and corcrash join combat_world SUPPORTED_UNITS using the existing cannon/rocket/guided-missile hosts (corraid was already supported). Native firing scripts match 1,617 snapshots (322/325/322/325/323); native_firing_reference now skips callbacks a script lacks (corthud/corstorm have no SetMaxReloadTime), leaving Arm traces unchanged. Tank origin (1,512) and target point (504) comparisons include the Core five. Real-map Core duels `--verify-<unit>` (auto-boot Core) pass for all five and are in NORMAL; the -Native block adds the Core firing oracles before origin/target. Beam-laser units (corgator, corfav, corak and Arm armfav) remain unsupported: no beam host exists. OPEN: corthud overshoots a Raider at 128 by ~4 units in height (barrels ~5 units above AimFrom torso; same geometry as the known Hammer barrel miss); its duel uses a 192 standoff. See CORE_COMBAT.md. Next candidates: beam-laser weapon host for both factions (native trace of beamweapon firing), resolving the Hammer/Thud short-range overshoot, Core defenses, Core-capable opponent.

Core mobile builders and simple structures: native_structure_reference.py/compare_native_structures.gd match 5,409/5,409 original-interpreter snapshots for corestor, cormstor, cordrag and cormine1..6 (healthy Create plus 600 idle ticks; see native-core-structure-validation.json), and those types join SCRIPTED_UNITS so Core builders can place them. test_mobile_builders now covers armcv/armck/armmlv plus corcv/corck/cormlv (102 checks) including unverified-entry rejection and Core storage capacity. `--core-builder-demo` (Core vehicle plant → corcv → corsolar) is in NORMAL; the structure oracle is in -Native; capture at local/core-builder.png was inspected. See MOBILE_BUILDERS.md. Still gated for Core: defenses, radar/sonar, geothermal, construction pad, air/naval/advanced factories, underwater/floating economy. Next candidates: Core T1 combat roster parity with the Arm roster (weapons already prepared in the bundle), Core defenses, then an opponent that can play either faction.

Core ground factories (resumed by Claude from the September 13 pause): the preserved corvp/corlab oracle edits were verified before use — compare_native_factory now matches 3,642/3,642 snapshots (759 each for corvp and corlab). native_mobile_reference covers all twelve Core T1 ground products; compare_native_units matches 7,320/7,320 (3,659 Core). construction_world.gd adds GROUND_FACTORIES (armvp/armlab/corvp/corlab) and the twelve Core products to SCRIPTED_UNITS. `ConstructionWorld.supported` gates Core types to verified scripts in placement and factory queues; the viewer shows other Core entries as disabled "(unverified)". test_factory_world covers all four factories (74 checks; the unpaid-debt check now removes the Commander because its settled income could legitimately pay a small request depending on phase) and test_produced_scripts covers all 24 products (144 checks). `--core-factory-demo` builds corvp and corlab with corcom, refilling stock before each (starting stock cannot fund both), produces one Raider and one A.K., and is in the NORMAL verifier; capture at local/core-factories.png was inspected. One full NORMAL verification passed. Opponent unchanged. No Core combat parity is claimed: corraid remains the only Core unit with world weapons; Core aircraft/naval/advanced factories, defenses and radar remain unverified and gated.

Viewer faction selection: the development viewer now accepts `--faction arm|core` and exposes Arm/Core buttons that reload the scene through `switch_faction`, retaining the requested faction across `reload_current_scene` via a SceneTree meta. The Core path routes commander model/COB script/movement fields/build menu/economy commander through `unit_catalog(commander_type)`; VIEWER_READY prints the selected commander with catalog-backed piece/texture counts. compare_native_cob.gd accepts `--core`, matches 464/464 corcom snapshots against the original bytecode without suppressing faults, and writes an aggregate summary to analysis/core-commander-native-validation.json (exe/COB SHA-256, instruction/static/piece counts, scenario_calls, host callbacks exercised, honest scope_limits). No raw trace or bytecode is committed. Existing Arm compare_native_cob still matches 464/464. Verified constructible Core buildings through the selector: corsolar/cormakr/cormex/corwin/cortide only. All other Core menu entries (corlab/corvp/corap/corsy/corllt/corrad/corsonar/coruwes/coruwms/coruwmex/corfmkr/cortl/corestor/cormstor) are exposed by the build picker but their factories, mobile production, weapons, radar/sonar and combat lifecycles are NOT verified and may fault or misbehave; this checkpoint does not claim a playable Core faction. test_faction_selection.gd headlessly boots Core, verifies model/script/economy/menu, exercises movement, builds a corsolar, then invokes `reload_current_scene()` while Core is selected and re-verifies the fresh scene (instance-id compared) still shows Core model, clean script, economy commander corcom and the resource-building menu entries; then flips through switch_faction to Arm and back to Core. tools/verify.ps1 runs it in NORMAL under `--quit-after 90` and the -Native block adds the corcom bytecode comparison. Rendered Core viewer capture at local/core-viewer.png. Opponent behavior is unchanged.

Core resource buildings: corsolar, cormakr, cormex, corwin, cortide now share the same healthy Create/Activate/Deactivate lifecycle as their Arm counterparts. native_solar_reference.py accepts a --unit flag for all ten (Core scripts also stub CACHE/SHADE vtable slots because their Create bodies use DONT_CACHE/DONT_SHADE, unlike armsolar). compare_native_solar.gd matches 606/606, 606/606, 609/609, 612/612, 606/606 snapshots for the five Core scripts and preserves the existing Arm flags/report paths. construction_world.gd exposes a single RESOURCE_BUILDINGS list covering both factions and adds the five Core types to SCRIPTED_UNITS. test_live_core_resources.gd builds each of the five through corcom begin_build with the correct water/land depth and, after isolating the building by removing the Commander, asserts exact per-settlement production (corsolar +20, cormakr initial-metal then deferred shortage/recovery, cormex extractor_yield-based income, corwin non-zero income once wind advances, cortide +20). Host-only Core Commander construction here is not full viewer Core faction support; the viewer still boots armcom.


Static feature blocking: prepare_map_metal.py also writes features.bin plus feature_blocking_version1 in metal.json. The launcher regenerates older bundles, and metal.bin is unchanged. terrain_navigation ORs the grid into each cell before footprint aggregation. The viewer Commander and every produced-unit cache receive it, and yard overlays keep it. native_feature_blocking.py matches 1,585 cases: 49 predicate cases and 1,536 layout cells versus feature_blocking.py; it is in -Native. Comet Catcher places only nonblocking moonmetal features, so its grid is all open; tests use synthetic grids. Unverified: TNT reserved-code loading (now rejected), overlap resolution, destruction/reclaim updates, projectile/feature collision and construction placement effects. See PLAYABLE_MOVEMENT.md.

Splash falloff: splash_damage.gd matches600 native0x49a2aa..0x49a3ee cases, six normalfixtures. Distance to translated axis-aligned unit bounds, truncate towholeunits, reject>=AoE/2, quadratic edgefalloff storedfloat32. RaiderAoE32=>radius16. Not world-integrated: nativebounds generation/targetenumeration/damage dispatcher still needed. See SPLASH_DAMAGE.md/native-splash-validation.json. Damage dispatch0x499cd0 has multipliertruncation thenattacker experience/globalflags; originalimpact0x499eb0 usesdirectunitdamage whenAoE<17.

AimFrom helper integration: weapon_queries.gd handles synchronous output, -1 AimFrom fallback to QueryPrimary(default0), fault checks and completion cleanup. Tank origin oracle now216/216 including72 original AimFrom calls and72 forced-missing fallbacks. Native harness now relocates COB name strings for name-based calls. Combat heading uses queried aim-origin position; normal queries6checks and combat regressions pass. Next environment/ballistic integration and lifetime/collision/splash for armed Raider.

Flash muzzle integration: piece_origin.model_origin matches72 real Flash/Raider firing query cases through original setters/traversal, four headings. Native setter movement direct, rotationlow16; name-order binding supported by loader inspection, original loader not executed. Combat now uses native-compared origins instead of renderer matrices. Normal combat and real-map destruction still pass. Host roll/pitch remainzero; rendererunchanged. See PIECE_ORIGINS.md/native-tank-origin-validation.json. Next AimFrom integration and cannon lifetime/collision/splash.

Native piece origins: piece_origin.gd matches600 original0x43def0 hierarchy cases with original0x4b7173 rotations. Six normal checks. Parent order Z/XY thenX/YZ thenY/XZ with integer nearest/even rounding each; root parent adds unit angles; finalZ negated. Distinct from current renderer matrices. See PIECE_ORIGINS.md/native-piece-origin-validation.json. Next verify real model/COB pose binding, then use simulation origins in combat muzzle/AimFrom calculations. Still unconnected intentionally pending that binding check.

Weapon minimum barrel angle prepared: runtime.minimum_barrel_angle, default-11.25deg ->float32 -0.19634954631328583rad. Nativeweapon suite now821/821 (618earlier+203anglecases);6normal scalar tests. Unit runtime schema2, launcher auto-upgrades, catalog rejects stale data. Bundle regenerated272units193weapons. Next: native muzzle/AimFrom world transforms and cannon lifetime/collision/splash, then integrate Raider firing. See WEAPON_SCALARS.md.

Cannon controller offset source recovered: initialization0x49e070 queries muzzle and AimFrom world coordinates; stored controller+0x10 is trunc(signed32(muzzleZ-aimZ)*1.25), NOT merely elapsed time. ballistic_launch.initial_offset matches300 native block cases; total launch comparison1300, normal launch checks14. Native piece-world transform still unverified. Aim minimum is weapon minbarrelangle default-11.25 degrees, scaled by0.017453292519943278 into float32 atweapon+c8; loader conversion inspection only. See updated BALLISTICS.md. Next connect query origins with verified transforms, prepare minimum angle, resolve lifetime/collision/splash for Raider.

Map gravity: native_map_gravity.py matches96 loader cases; map_environment.py recovers default/override and x87 scale. Comet Catcher is gravity60 ->raw4369, winds10..15 (not default8155 used by isolated fixtures). prepare_viewer now reads OTA and writes scene.environment +environment_version1; launcher upgrades older bundles. Six normal tests. See analysis/BALLISTICS.md and native-map-gravity-validation.json. Remaining integration: controller accumulator/min-angle evolution, wind initialization/scheduling, lifetime/collision/splash before armed Raider.

Wind update: wind_state.gd matches 600 native 0x490c40 cases including both original RNG final states, timer gating, strength/heading, quantized X/Z drift and float32 ratio; 8 normal checks. Oracle replaces only CRT thread-storage lookup, not randomness. See analysis/WIND.md and native-wind-validation.json. Not world-scheduled yet: initial normalization, seeds, Y drift initialization and shared random consumers remain. Ballistic controller accumulator/map gravity/lifetime/splash still block faithful armed Raider integration, not work overall.

Ballistic launch velocity: ballistic_launch.gd matches 1,000 native cases at 0x49ce4f..0x49cecc, using original integer trig plus gravity * unsigned(controller+0x10)/speed vertical correction. Six normal native fixtures. See analysis/BALLISTICS.md and native-ballistic-launch-validation.json. Controller accumulator evolution is still untraced; do not assume always zero. Drift writer located at wind update0x490c40: X/Z=-2*sin/cos(wind heading+0x37ed8,strength+0x37eda). Next recover controller state/map gravity/wind and lifetime/splash, then integrate armed Raider.

Ballistic aiming: ballistic_aim.gd matches 1,236 unmodified native 0x49a890 calls (494 accepted), including adjacent raw distances at maximum reach. Source-minus-target coordinates, positive speed/gravity, minimum angle; returns unsigned COB pitch or 0x8000 failure. Nine normal native-derived fixtures. See analysis/BALLISTICS.md and native-ballistic-aim-validation.json. Next remains launch 0x49cde0, map gravity/drift, lifetime and splash before armed Raider integration. Current binary64 reconstruction is verified for tested cases, not a general x87 precision proof.

Ballistic primitive: 1,120 native cases match the original live ballistic update (1,000 varied inputs +120-step arc). New ballistic_motion.gd moves by velocity plus supplied game drift before subtracting gravity; signed32 wrap preserved. Five normal checks, optional native oracle/comparator in verify.ps1. See analysis/BALLISTICS.md for addresses and boundaries. Not connected to world firing yet. Next trace aim 0x49a890, launch 0x49cde0, map gravity loading and drift derivation, then lifetime/collision/splash. Do not reuse the EMG path-distance lifetime for shells without native evidence.

Core Raider firing prerequisite: 325 native interpreter snapshots match for healthy Create/aim/query/fire/recoil plus two HitByWeapon rocking callbacks. `native_firing_reference.py --unit corraid` writes local/firing/corraid; `compare_native_firing.gd -- --corraid` compares it. Normal weapon-cycle checks now 18, including sustained Raider single-shot events. Raider added to healthy SCRIPTED_UNITS so practice-target muzzle visibility/poses are script-driven. No Raider world firing yet: CORE_LIGHTCANNON is ballistic=1, range240, damage50, AoE32, speed raw371370 and reload45 ticks. Next: reconstruct ballistic trajectories/aiming and splash before armed Raider opponent; do not silently use straight EMG geometry. See analysis/FIRING_CYCLE.md and native-raider-firing-validation.json.

Latest playable combat checkpoint: a selected factory-produced Flash can attack a stationary Core Raider practice target using queried muzzle poses, EMG burst callbacks, integer projectile integration, provisional swept sphere hits, health and removal. Use Add practice target, then click the red-ringed target; Stop/move cancels. 18 combat host checks and --verify-combat pass. --combat-demo renders a live firing scene. See analysis/COMBAT.md for full limits: no spray, splash, armor, damage COB callbacks, native collision fidelity, original death effects or AI yet. This supersedes older notes saying combat is not connected. Next: enemy behavior plus missing weapon/damage fidelity and Core coverage.

Firing prerequisite: Flash QueryPrimary/FirePrimary overlap and recoil match 323 native snapshots, including actual query output locals. First three supplied muzzle queries are 0,0,1; do not hard-code alternation. `weapon_cycle.gd` emits script-queried shot events with provisional burst/reload timing and re-aims after idle restoration/permission pauses. Fourteen host checks pass. It is not yet wired to the world; next is projectile spawn transforms/spread, collision and damage. See analysis/FIRING_CYCLE.md.

Combat prerequisite: exact loader conversions for weapon speed, reload ticks and burst interval are now prepared into each weapon's local `runtime` data. 618 native cases match; see analysis/WEAPON_SCALARS.md. x87 precision matters (.3 seconds => 8 ticks; EMG speed 300 => raw 655359/tick). `unit_catalog.weapon(name)` exposes these values; launcher upgrades old bundles. Next combat work is firing callback/burst scheduling, projectiles, hits and damage; no combat is playable yet.

Mobile-builder extension: Arm Construction Vehicles, Construction Kbots and Minelayers now expose original menus and independent construction jobs. StartBuilding/readiness/StopBuilding, range, pause/resume and move cancellation are integrated. 48 host checks and a real-map factory-produced Construction Vehicle building solar pass; see analysis/MOBILE_BUILDERS.md. Normal verifier includes --builder-demo. Older notes saying mobile-builder commands are missing are superseded. Combat/opponents and Core coverage remain the next major gameplay work.

Latest extension: all twelve Arm Vehicle Plant/Kbot Lab products execute healthy original COB scripts. Native playback matches 3,661 snapshots; 73 host checks cover production, animation and exit for every product. Odd-sized footprint alignment in the yard overlay was fixed to agree with terrain_navigation. Both real-map factory demos pass. See analysis/MOBILE_SCRIPTS.md; this supersedes older notes about static Kbot poses. Next major gameplay work remains mobile-builder controls, combat and opponents, alongside Core coverage and fidelity work.

- Installation inventory and PE inspection in `analysis/`.
- Ghidra 12.1.3 project at `local/ghidra-projects/TotalAnnihilation.gpr`.
- 2,640 functions decompiled; one failed at `0x004e5392`. Approximate output, not buildable source.
- Archive directory catalog, HAPI extraction, selected TNT/3DO/GAF conversion.
- Working Godot viewer: Comet Catcher and textured Arm Commander, now driven by original COB bytecode.
- COB decoder and disassembler; all four installed Commander script variants are identical, no loose override.
- Scene-independent GDScript runtime implementing the Commander-used instruction subset, eight slots, 32-word stacks, 30 Hz timing, child-call waits, signals, and integer motion.
- 58 runtime checks, 14 Python parser tests, and viewer integration checks pass.
- Stronger test: isolated Unicorn execution of original x86 interpreter matches all 464 state snapshots over 450 ticks. Reproducible with `tools/verify.ps1 -Native`; see `analysis/COB_VM.md` and `analysis/native-cob-validation.json`.
- Native comparison found and corrected the already-at-target MOVE velocity edge case.
- `local/commander-walk.mp4` captures 60 rendered frames of script-driven motion.
- Validation and known limitations: `analysis/ASSET_FORMATS.md`.

## Run

Double-click `Run Viewer.cmd`, or run `tools/run_viewer.ps1`.

Prepare again: `python tools/prepare_viewer.py`.

Normal checks: `tools/verify.ps1` (Python parser tests, runtime checks, viewer checks).

Godot checks: `Godot_v4.6.2-stable_win64_console.exe --headless --path godot -- --verify`.

Screenshot: run Godot with `--path godot -- --capture ABSOLUTE_PNG_PATH` without `--headless`.

## Active goal and next engineering milestone

The user explicitly set the active goal to a playable version of the entire Total Annihilation game. A Codex goal is active; do not mark it complete at intermediate checkpoints. Continue autonomously through playable systems and preserve the fidelity evidence/limitations distinction. No token budget was requested. No subagents were authorized.

Commander map movement is now available: click terrain to move, right-click/S to stop. Native ground steering (0x0043cd20) matches 1,200 additional supplied-waypoint cases, bringing movement comparison to 6,837 checks. `terrain_navigation.gd` supplies a provisional footprint-aware A* route; `mobile_unit.gd` integrates this with the original script. Twelve navigation checks and a real-map viewer integration run pass; actual Commander goes from (3072,3840) to (3199.673,3744.345), stops and settles its original pose. `tools/verify.ps1 -Native` passes all existing checks too. See `analysis/PLAYABLE_MOVEMENT.md` and `analysis/GROUND_MOVEMENT.md`.

Current next work: broader unit script coverage and mobile builders, Core factories, weapons/projectiles/damage and opponents. Work toward an actual playable skirmish loop while continuing native terrain/collision research. Do not describe the provisional A* and sampled height as faithful original navigation. Pitch remains zero; map feature collision, class-vs-FBI rules, terrain projection and full economy fidelity remain unfinished.

Update: the original-data bundle and shared visuals are now implemented. `tools/prepare_units.py` prepares 272 units, 193 weapons, 450 textures, 45 builder menus, 566 build relationships. Nested `tools/tdf.py` parses actual data; `unit_catalog.gd` caches it; `unit_visuals.gd` shares GPU mesh/material/texture resources while keeping independent poses. Viewer uses this renderer. All 272 models instantiate; 8,924 catalog checks and full native suite pass. See `analysis/UNIT_BUNDLE.md` for source-selection limits and construction-field offsets. Next concrete work is construction placement/progress/resource accounting. Duplicate model names in armch/armss/corss need original binding verification. Programs decode but VM engine-callback/opcode support for all units is not complete.

User refinements: full gameplay rebuild first, graphical improvements afterward, specifically improved explosions while retaining classic presentation. Raise unit capacity reasonably and make it configurable; user explicitly softened 10,000 into a benchmark-driven target. Do not promise a numeric playable limit without full combat/pathfinding/AI load tests. Current shared meshes are a foundation, not a performance benchmark.

Construction update: first playable loop is now in `construction_world.gd` and viewer: dropdown placement, costs, partial progress, stop/resume, completion, completed-unit production/storage. Original positive-work routine 0041ba60 and request routine 004011c0 match 1,000 native cases through `construction_math.gd`; use float32-bit comparisons (JSON decimal formatting caused false mismatches initially). Host work timing, immediate spending, placement/range and production are provisional. Fifteen host tests and real-map solar completion pass. Building models remain static without their COB programs, so solar panels do not animate open. See `analysis/CONSTRUCTION.md`. Next: building script callbacks/activation, resource settlement, factory production, selection and combat.

Strict reasoning preference: user authorizes ONLY Light/low and Medium. Evaluate every step; use Light even if slower. Medium only for a concrete difficult reasoning problem Light cannot reliably resolve, then return immediately to Light. Switching via `mcp__codex_app__send_message_to_thread` to this same task with `thinking` low/medium was tested; user confirmed the UI switch worked. Current requested setting is Light. Do not repeat the test or queue recursive switching messages. Announce actual necessary switches briefly. No other reasoning modes or model substitutions are authorized.

Native movement oracle stubs route-provider virtual calls, not the steering routine. Initialize x87 to FINIT state; Unicorn defaults caused native CRT exception paths before this was corrected. `capstone==5.0.7` is available in ignored local/python-deps for disassembly, but not needed to run the oracle. Route source has three points: previous, target, final. Exact path-provider meanings/selection remain to be traced. Disassembly excerpt is ignored `local/movement/ground-steering.asm`.

General archive lookup precedence remains unresolved; only its registration order was observed. The chosen Commander script is unaffected because all copies are identical. Do not infer stale BOS source solely from numeric differences: compiler unit scaling has not been reconstructed.

The user supplied GitHub repository `https://github.com/supertechieg/Total-Annihilation-Remake.git` and explicitly requested origin/main/push setup. Keep original binaries, extracted assets, tools, decompiled pseudocode, and raw extracted strings out of the source checkpoint (ignored locally).

Latest checkpoint: playable Arm factory queues, pad production, automatic exit and selected-unit movement are now implemented. See `analysis/FACTORIES.md` for complete scope and remaining fidelity gaps. The normal suite includes 36 factory host checks plus a real-map Commander-built Vehicle Plant producing two Flash tanks and moving a selected tank. Solar animation has 606 native snapshots; Arm factory/rotation has 2,124. The host uses provisional yard overlays, immediate property-18 readback, a fixed unrotated exit direction and cached per-type navigation. Dynamic unit avoidance, other product animations, mobile-builder controls and actual combat remain unfinished. The older construction/bundle paragraphs above describe prior checkpoints; this paragraph and current source/tests supersede their outstanding-work lists.
